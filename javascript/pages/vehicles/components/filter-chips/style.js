import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    paddingTop: '8px',
    paddingBottom: '8px',
    alignItems: 'center',
    flexDirection: 'row',
    overflowX: 'scroll',
  }
})

export default styles;